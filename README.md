# artemmyassyst Bot

🤖 Telegram бот для работы с недвижимостью в Сочи с AI-агентом "Алёна"

## Описание

AI-ассистент для консультации по недвижимости в Сочи с интеллектуальной системой памяти.

## Возможности

- 🤖 **AI-агент "Алёна"** - персональный менеджер по недвижимости
- 🧠 **OpenAI/Anthropic** - умные ответы через ведущие LLM
- 💾 **ZEP Cloud** - интеллектуальная система памяти
- 📝 **Система логирования диалогов** - полное отслеживание всех разговоров
- 🚀 **Railway** - автоматический деплой
- 📊 **Google Sheets** - интеграция для CRM
- 🔗 **Webhook** - мгновенные ответы
- 🔍 **Анализ диалогов** - инструменты для изучения эффективности бота

## Быстрый старт

### 1. Настройка переменных окружения

Создайте файл `.env`:

```env
TELEGRAM_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN_HERE
BOT_USERNAME=@artem_integrator_bot
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE
ZEP_API_KEY=YOUR_ZEP_API_KEY_HERE
WEBHOOK_SECRET_TOKEN=YOUR_WEBHOOK_SECRET_HERE
ADMIN_PASSWORD=YOUR_ADMIN_PASSWORD_HERE
```

### 2. Локальный запуск

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск webhook сервера
python webhook.py
```

### 3. Деплой на Railway

1. Подключите GitHub репозиторий к Railway
2. Установите переменные окружения в Railway Dashboard
3. Railway автоматически задеплоит проект

## Управление ботом

### Основные эндпоинты

- `/` - Health check и статус бота
- `/webhook` - Webhook для Telegram
- `/set-webhook` - Установка webhook
- `/admin/reload-prompt` - Перезагрузка инструкций
- `/admin/sheets/sync` - Синхронизация Google Sheets

## Конфигурация

### Инструкции для LLM

Инструкции хранятся в `data/instruction.json`:

```json
{
  "system_instruction": "Ваша системная инструкция...",
  "welcome_message": "Приветственное сообщение",
  "last_updated": "2025-01-27T12:00:00"
}
```

### Основные настройки

- `bot/config.py` - Конфигурация бота
- `requirements.txt` - Python зависимости  
- `Dockerfile.complete` - Docker контейнер
- `railway.json` - Настройки Railway деплоя

## Особенности

### Интеллектуальная система памяти

- **ZEP Cloud** - продвинутая система запоминания диалогов
- **Квалификация лидов** - автоматическая оценка потенциала клиентов
- **Система состояний** - отслеживание этапа диалога
- **Google Sheets** - синхронизация данных для CRM

### Мониторинг и отладка

- Ротируемые логи в `logs/bot.log`
- Health check для проверки статуса
- Structured logging для аналитики

## Архитектура

- **webhook.py** - FastAPI веб-сервер для Telegram webhook
- **bot/agent.py** - AI-агент с системой памяти
- **bot/memory/** - модули интеллектуальной памяти
- **bot/integrations/** - интеграции с внешними сервисами

## Система логирования и анализа диалогов

### 📝 Логирование диалогов

Бот автоматически сохраняет все диалоги в файлы JSONL формате:

- **Каталог**: `logs/dialogs/`
- **Формат файлов**: `dialogs_YYYY-MM-DD.jsonl`
- **Содержимое**: Все сообщения пользователей и ответы бота с метаданными

#### Структура лога:
```json
{
  "timestamp": "2025-09-24T10:29:24.625427",
  "session_id": "user_12345",
  "user_id": "12345",
  "user_name": "Иван Иванов",
  "message_type": "user|assistant",
  "text": "Текст сообщения",
  "state": "s1_business",
  "qualification": "cold|warm|hot",
  "metadata": {...}
}
```

### 🔍 API для просмотра диалогов

Доступны следующие эндпоинты для анализа:

#### Статистика диалогов
```bash
GET /admin/dialogs/stats
```

#### Диалог конкретного пользователя
```bash
GET /admin/dialogs/user/{user_id}?limit=20
```

#### Последние диалоги всех пользователей
```bash
GET /admin/dialogs/recent?limit=50
```

#### Поиск по диалогам
```bash
GET /admin/dialogs/search?query=недвижимость&user_id=12345
```

#### Экспорт диалога
```bash
GET /admin/dialogs/export/{user_id}?format=json|txt
```

### 🛠️ Анализ диалогов

Используйте скрипт `analyze_dialogs.py` для детального анализа:

#### Общий отчет за 7 дней
```bash
python analyze_dialogs.py
```

#### Поиск проблемных диалогов
```bash
python analyze_dialogs.py --problems-only
```

#### Анализ конкретного пользователя
```bash
python analyze_dialogs.py --user 12345
```

#### Сохранение отчета в файл
```bash
python analyze_dialogs.py --output report.txt
```

#### Анализ за определенный период
```bash
python analyze_dialogs.py --days 30
```

### 📊 Возможности анализа

Скрипт анализа предоставляет:

1. **Статистику состояний диалогов** - переходы между S0-S8
2. **Анализ квалификации клиентов** - COLD→WARM→HOT
3. **Поиск проблем**:
   - Повторные приветствия
   - Зацикленные состояния
   - Потеря контекста
4. **Метрики эффективности**:
   - Среднее время в каждом состоянии
   - Коэффициенты конверсии
   - Частота переходов

### 🔧 Устранение неполадок

#### Если бот повторяет приветствия:
1. Проверьте логи: `python analyze_dialogs.py --problems-only`
2. Убедитесь в корректности ZEP API ключа
3. Проверьте локальное кэширование в `memory_service.py`

#### Если теряется состояние диалога:
1. Проверьте создание новых сессий в логах
2. Убедитесь что `existing_session_id` передается правильно
3. Проверьте работу ZEP Cloud API

## Исправленные проблемы

### Версия 2.1 (сентябрь 2024)

✅ **Исправлена проблема повторных приветствий**
- Добавлена система локального кэширования состояний
- Исправлена передача `existing_session_id` в webhook.py
- Улучшена обработка сбоев ZEP API

✅ **Добавлена система логирования диалогов**
- Полное логирование всех сообщений и переходов состояний
- API для быстрого просмотра логов
- Инструменты анализа эффективности бота

✅ **Улучшена система памяти**
- Многоуровневое кэширование (локальный + ZEP)
- Автоматическое восстановление при сбоях API
- TTL для локального кэша (1 час)

## Версия

Версия: 2.1 (с системой логирования диалогов)

---

*Создано автоматически системой клонирования ботов* 🤖